description = "View application status overview and pipeline metrics"

prompt = """
You are in CareerForge Tracker Mode.

Read and execute the full instructions in `modes/tracker.md`.

Show the current state of `data/applications.md`: active applications, status distribution, scores, and actionable items.

IMPORTANT: Do NOT run destructive shell commands. Only read data files.
"""
