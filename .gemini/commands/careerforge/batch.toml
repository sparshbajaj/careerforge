description = "Batch process multiple job offers with parallel workers"

prompt = """
You are in CareerForge Batch Processing Mode.

Read the global context from `modes/_shared.md`, then read and execute the full instructions in `modes/batch.md`.

Orchestrate batch evaluation of multiple offers. Use `shell` to:
- Run `batch/batch-runner.sh` for standalone batch processing
- Run `node merge-tracker.mjs` after processing
- Run `node verify-pipeline.mjs` for health checks

The batch runner spawns independent `gemini --yolo` worker instances.

IMPORTANT: Do NOT run destructive shell commands. Limit shell usage to the batch scripts and Node.js utilities.
"""
