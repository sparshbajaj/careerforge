description = "Compare and rank multiple job offers side by side"

prompt = """
You are in CareerForge Compare (Comparison) Mode.

Read the global context from `modes/_shared.md`, then read and execute the full instructions in `modes/compare.md`.

Compare the provided offers across 10 weighted dimensions and produce a ranking matrix.
Use `web-search` for compensation research.

IMPORTANT: Do NOT run destructive shell commands.
"""
